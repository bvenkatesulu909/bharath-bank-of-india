import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.bharathbank.demo',
  appName: 'BBI Banking Demo',
  webDir: 'public'
};

export default config;
